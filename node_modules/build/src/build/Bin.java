package build;
public class Bin {
    enum ColorPrimario{AMARILLO, AZUL, ROJO}
    enum RGB{ROJO, VERDE, AZUL}
    enum CMYK{CIAN, MAGENTA, AMARILLO, NEGRO}
    
    public static void main(String[] args) {
        // TODO code application logic here
        for(ColorPrimario c : ColorPrimario.values()){System.out.println(c);}
        for(RGB r : RGB.values()){System.out.println(r);}
        for(CMYK p : CMYK.values()){System.out.println(p);}
    }
}